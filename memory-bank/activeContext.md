# Contexto Activo

## En qué estamos trabajando ahora
- Configuración inicial y arranque técnico del proyecto tras haber establecido la arquitectura de carpetas y el banco de memoria.

## Próximos pasos inmediatos
1. Crear el archivo `requirements.txt` en la carpeta `backend/`.
2. Escribir la configuración de la base de datos (`database.py`) y los modelos ORM de SQLAlchemy.
3. Inicializar el proyecto de React con Vite y configurar Tailwind CSS.